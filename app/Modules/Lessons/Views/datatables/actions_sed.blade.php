<a href="{{ route('lessons.hello.edit', $item->id) }}" class="btn btn-warning btn-sm">
    <i class="fas fa-edit"></i>
</a>

<a href="{{ route('lessons.hello.delete', $item->id) }}" class="btn btn-danger btn-sm">
    <i class="fas fa-times"></i>
</a>